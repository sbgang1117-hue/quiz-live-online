# QUIZ LIVE v7.5.0

v6.6.0을 기반으로 한 대형 기능 패치입니다.

## v7 패치노트

### 새로 구현
- **배경 이미지 직접 업로드**: URL 입력 대신 JPG/PNG/WebP/GIF 파일을 선택해 서버에 업로드합니다. 최대 5MB.
- **문제 이미지도 서버 업로드 방식으로 통일**: 기존 Base64 방식 대신 `/uploads` 파일 URL을 사용합니다. 기존 Base64 이미지가 남아 있으면 조회 시 자동 마이그레이션합니다.
- **문제 편집 편의성 강화**
  - 문제 위/아래 이동
  - 문제 복제
  - 마지막 문제 삭제 방지
- **JSON 백업/복원**
  - 현재 퀴즈 JSON 내보내기
  - JSON 파일 불러오기
  - 가져온 퀴즈는 새 퀴즈로 저장할 수 있습니다.
- **대기실 QR 코드**: 서버에서 QR SVG를 생성해 방 입장 링크를 바로 스캔할 수 있습니다.
- **호스트 문제 건너뛰기**: 진행 중인 문제를 즉시 정답 공개로 넘기거나 공개/다음 문제 단계에서 다음 문제로 이동할 수 있습니다.
- **최종 통계**
  - 참가자별 정답 수
  - 정확도
  - 평균 응답 시간
  - 문제별 응답 기록
- **테마 배경 실제 적용**: 저장한 배경 이미지가 라이브 게임/공개 화면에 적용됩니다.
- **기존 기능 유지**: 음악 파일 서버 업로드, 20MB 제한, 멀티유저 음악 퀴즈, YouTube 구간 재생, O/X/주관식/복수선택/순서맞추기, 아바타, FEVER/PLATINUM, 점수 공개 타이밍, 재접속 토큰 등을 유지합니다.

## 안정성 점검
- `server.js` Node 문법 검사 통과
- `public/app.js` Node 문법 검사 통과
- v6.6의 Socket.IO 2MB 제한은 그대로 유지해 대용량 미디어가 WebSocket으로 다시 흘러들어가는 문제를 방지합니다.
- 이미지/음악은 HTTP multipart 업로드 후 URL만 실시간 퀴즈 데이터에 저장합니다.
- 재접속 참가자의 과거 통계를 유지하도록 통계 식별자는 소켓 ID가 아닌 재접속 토큰을 사용합니다.

## 실행
```bash
npm install
npm start
```

Postgres를 사용하려면 `DATABASE_URL`을 설정합니다. 설정하지 않으면 `data/quizzes.json`을 사용합니다.

> 주의: 현재 `/uploads`는 서버 로컬 파일 저장 방식입니다. 호스팅 환경에서 재배포/재시작 시 파일이 초기화되는 서비스라면 추후 S3/R2/Supabase Storage/Cloudinary 같은 영구 스토리지로 교체하는 것이 좋습니다.


## v7.3 패치
- 콤보 시스템 ON/OFF 및 제작자용 설명 팝업
- 일반 / 서바이벌 / 스피드런 게임 모드
- 서바이벌 탈락 처리 및 마지막 생존자 우선 결과
- 스피드런 전체 응답 즉시 공개
- 문제 카드 데스크톱 드래그 정렬 + 모바일은 기존 위/아래 버튼 유지
- 최종 결과 고급 통계 화면
- 문제별 정답률/응답수/평균응답시간, 난이도/속도 TOP, 참가자별 통계
- 기존 v6.6 미디어 업로드 및 실시간 Socket.IO 구조 유지


## v7.3 신규
- 팀 모드: 입장 시 RED/BLUE 선택, 팀원 목록/팀 정확도 표시
- 스피드런: 참가자별 독립 문제 진행, 즉시 다음 문제, 속도 티어 점수, 실시간 러너/GOAL 표시
- 하이 리스크 옵션: 5초 겁쟁이/도전 선택, 도전 정답 2배·오답 기본점수 차감
- 레이드 옵션: 일반/팀 외 모드에서 8지선다, N명/N% 성공 기준, 성공 시 정답자 지급 및 상위 기준 명단 연출, 실패 시 정답 미지급·오답 기본점수 차감
- 스피드런에서는 하이 리스크/레이드 자동 비활성화, 팀 모드에서는 레이드 자동 비활성화


## v7.4 패치
- 데스 모드로 명칭 변경 및 생존자 1명 즉시 우승 / 전원 탈락 시 우승자 없음
- 데스 모드 생존자 그리드와 탈락 상태 표시
- 팀 모드 참가자 자동 RED/BLUE 균등 배정
- 게임 시작 대기 화면에 모드별 설명 및 개인 컬러 디자인 추가
- 출제 화면 모드/옵션에 커스텀 ? 도움말 추가
- JSON 불러오기/내보내기 도움말 추가
- 문제 대기 화면에 레이드/하이리스크/FEVER/PLATINUM 설명 표시
- 스피드런은 레이드/하이리스크 미지원, 팀은 레이드 미지원, 데스는 레이드/하이리스크 미지원
- 기존 서바이벌 퀴즈는 데스 모드로 자동 호환
- 스피드런 답안 제출 라우팅 오류 수정


## v7.5.0
- 게임 모드 DB 저장/런타임 적용 보강
- 팀 자동 균등 배정
- 하이 리스크 조기 시작 및 선택 상태 표시
- 레이드/하이 리스크 상호 배타
- 데스 모드 실시간 생존 그리드/탈락 카드
- 스피드런 실시간 사이드 랭킹/누적 경과시간
- 수정 후 바로 방 만들기 자동 저장
